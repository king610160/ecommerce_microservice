apiVersion: v1
kind: Service
metadata:
  name: {{ include "auth-service.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
  selector:
    app: {{ include "auth-service.name" . }}

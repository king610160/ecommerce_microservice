apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend.fullname" . }}
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
  selector:
    app: {{ include "frontend.name" . }}